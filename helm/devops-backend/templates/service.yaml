apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
spec:
  type: NodePort
  ports:
    - port: 5002
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: devops-backend
