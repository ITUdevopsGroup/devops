apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops_backend.fullname" . }}
  labels:
    {{- include "devops_backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops_backend.selectorLabels" . | nindent 4 }}
