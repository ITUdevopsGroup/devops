apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-backend.fullname" . }}
  labels:
    {{- include "devops-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-backend.selectorLabels" . | nindent 4 }}
