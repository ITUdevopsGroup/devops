apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops_frontend.fullname" . }}
  labels:
    {{- include "devops_frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops_frontend.selectorLabels" . | nindent 4 }}
